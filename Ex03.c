#include <stdio.h>

int main(){
	int a = 4, b = 5;
	int c = a + b;
	int hieu = a - b;
	int tich = a * b;
	int thuong = a / b;
	
	printf("tong la %d\nhieu la %d\ntich la %d\nthuong la %d", c, hieu, tich, thuong);

	return 0;
}
